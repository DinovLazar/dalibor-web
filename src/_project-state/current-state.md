# current-state.md

> **Location in repo:** `src/_project-state/current-state.md`
> A live snapshot of the repo. **Claude Code updates this at the end of every phase.** It reflects what actually exists — if it ever disagrees with the Plan, this file (and the live code) wins.

**Last updated:** 2026-07-06 — **Phase 2.01g (Code) — reviewed-book covers downloaded, validated & manifested; Sanity import DEFERRED (no write token on this machine).** Downloaded + verified **19 of 20** reviewed-book covers (all genuine flat portrait covers, JPEG magic-byte-confirmed, aspect 1.42–1.67) into `content-packet/assets/reviewed-books/`, and extended `content-packet/assets/assets.json` `reviewed_books[]` to **20 entries (19 `use:true` + 1 held)** with mk/en/sr alt, per-title source/dimensions/rights + notes. **#11 „Во хаосот радост" (Kalija Dimitrova) HELD** (`file:null`, `use:"hold"`) — self-published, no flat cover exists. **3 sub-350 phase-table URLs (#6 Chinook, #8 Kao da nema sutra, #13 Albert — all 296px `getmedia`/Ljevak masters) were UPGRADED, not held**, to larger copies of the *same* VBZ artwork (Znanje 360×526; vbz.hr 674×1000 & 599×1000). **#19 „Svijet je gladno mjesto" accepted at 300×455** (settled sub-floor exception). Provenance quirks: iliili.com.mk (#3, #16) fetched via a pinned host IP (curl couldn't resolve it); durieux.hr (#18) via plain HTTP (cert didn't verify — one-time, nothing insecure ships). **`Буники`: product URL NOT found within cap** — literatura.mk's live PNV Публикации catalog (walked across every category view + search endpoints + Google index) does not list it today → `book_cover` left **held** (no candidate to show) and `book.purchaseLinks` left at the site-root URL (exact-URL patch NOT applied; no step-4 Sanity write). Manifest validated: 20 entries, `docId = review-<slug>` mapping 1:1 onto all 20 reviews, no dupes, `alt.mk` everywhere, **Zaporožac scrub 0 hits**. Pre-flight: all 20 `review-*` docs confirmed present in `production`. `typegen` (no diff) / `lint` / clean `build` (**98 pages**, no regression) pass. **DEFERRED (needs `SANITY_WRITE_TOKEN`):** `npm run import:assets` (+ idempotency re-run), the GROQ cover spot-check, list/aside cover rendering, live-site confirm, axe. Operator chose **commit-without-import**; the clone (the stale `Projects/dalibor-web` checkout — ran here per operator go-ahead) was **synced to `origin/main` first**. Branch **`phase/2.01g-reviewed-covers`** pushed; **merge to `main` pending Lazar** (covers only render once the import runs with the token). Carryovers refreshed — Dalibor's review/post prose; the **real `Буники` cover + exact purchase link remain unresolved** (not on literatura.mk today); alt-text polish. Report: `Part-2-Phase-01g-Completion.md`. — *(Prior: 2026-07-03)* **Phase 2.01e (Code) complete — Dalibor's real author portrait is now LIVE on the site + the browser-tab icon is his face.** Uploaded his interim **author portrait** (720² Tower Bridge self-portrait, his canonical public photo across Versopolis + Booksa) to Sanity and set **`author.photo`** with alt in mk/en/sr — it now renders on **About** (4:5 frame, via `next/image` + the Sanity CDN) and the Home hero. The **`Буники` book cover is HELD** (Cowork found no genuine flat cover online — only an event promo poster; `book_cover.use="hold"`, no file) so the Book page keeps its tasteful placeholder, awaiting Dalibor's real cover. Reviewed-book/blog/banner images are intentionally empty (deferred). Set the **site favicon**: new App-Router `src/app/icon.png` (512²) + `apple-icon.png` (180²) generated from `avatar-square.jpg` via `sharp`; the framework's default `src/app/favicon.ico` and the 5 unused scaffold SVGs (`vercel/next/file/globe/window.svg`) were deleted — the built HTML now links `/icon.png` + `/apple-icon.png` and `/favicon.ico` is gone (404). Shipped: an idempotent **`scripts/import-assets.mts`** (`npm run import:assets`) mirroring `import-content.mts` — uploads each `use:true` image and sets the `localizedImage` field via **patch** (not `createOrReplace`), **preserves any already-set field** (skip unless `--force`), `--dry-run`, the Zaporožac scrub, a `uploaded/set/skipped/held` tally; + **`scripts/make-favicon.mts`** (`npm run make:favicon`) + **`sharp@0.34.5`** pinned in devDeps (favicon-only, not shipped). Run tally: `uploaded=1 set=1 skipped=0 held=1`; a 2nd run is a genuine **no-op** (`skipped(already-set)=1`) — idempotent. Verified: `author.photo.asset._ref` set + `alt.mk` non-empty (fetch); `typegen` (no diff — image fields pre-existed) / `lint` / `rm -rf .next && build` all clean (**98 static pages**, +1 vs 2.01c's 97 = the icon-route swap (−1 `favicon.ico`, +2 icon/apple-icon), **not** a content regression); About portrait renders live; **axe on About + Book = 0 content violations** (only the 10 documented footer `text-on-footer` `color-contrast` bg-detection false-positives, real cream-on-walnut ≈8:1); no console errors. **No deploy / no Vercel env / no schema change.** Branch **`phase/2.01e-photos`** (pushed); **merge to `main` pending Lazar's go**. Report: `Part-2-Phase-01e-Completion.md`. — *(Prior: 2026-07-03)* **Phase 2.01c (Code) complete — Dalibor's REAL reviews, blog post & topics are now LIVE in `production`; the site is placeholder-free.** Imported **20 reviews + 1 blog post** (copyright-safe: title + reviewed book + topics + "first published on Booksa" `source` link; **no body/excerpt** — Dalibor pastes prose in the Studio). **Key divergence handled:** the live dataset had drifted from the 2.01b snapshot — instead of 12 `[PLACEHOLDER]` seed docs it held **Dalibor's own 14-topic taxonomy** (ids `t-<slug>`, hand-built in the hosted Studio since 2.04) + 0 reviews/posts/placeholders. Importing the packet's 13 `topic-<slug>` docs as-written would have created **27 duplicate-concept topics**, so I **halted before any write and asked**; **operator (Lazar) chose to reconcile onto the live `t-*` taxonomy.** The importer now **maps** each packet topic slug → Dalibor's existing `t-*` id (11 of 13; `women-and-gender`→`t-womens-writing`) and creates only the **2 missing** concepts (`t-essay`, `t-society-politics`), never modifying his topics → dataset = **16 topics** (his 14 + 2), reviews/posts reference 13. Import verified: `review:20 · post:1 · [PLACEHOLDER]:0 · dangling refs:0 · 2/2 new topics`; **idempotent** (2nd run = packet `skipped=23`, no churn). Shipped alongside: `post.source` schema field via a new shared **`sourceField()`** factory (reused by `review` + `post`; `review` schema byte-identical) + typegen + the quiet **single-post "Source" attribution** (new `blog.source` string in mk/en/sr); the **Reviews list now filters topic chips to used topics** (mirrors Blog) so Dalibor's 3 unused topics + post-only tags don't render as dead chips; `content-packet/{topics,reviews,posts}.json` written verbatim + `README.md` reconciliation note. `rm -rf .next` → `typegen`/`lint`/`build` clean (**97 static pages**, was 58); axe 0 violations on Reviews list + a single review + the blog post; no console errors; code-review subagent pass (no blocker/high). **No embeddings backfill** (Voyage payment still pending — the 20 new reviews are unembedded; keyword-search fallback by design). **No deploy / no Vercel env touched.** Report: `Part-2-Phase-01c-Completion.md`. — *(Prior: 2026-06-26)* **Phase 2.01b (Code) PARTIAL pass complete — Dalibor's REAL Author + Book singletons + the 8 translations are now LIVE in `production`** (the `[PLACEHOLDER]` singletons are gone; bio §1 + book description §2 imported verbatim from intake; education + `Буники`/`Bunike` + publisher `ПНВ Публикации` + year 2022 (dossier-sourced) + Литература.мк link + `genre = збирка раскази` data-only). **The Cowork content packet (reviews/posts workbook + topics docx + assets) was never committed and is absent**, so reviews/posts/topics/covers + the `[PLACEHOLDER]` cleanup are **DEFERRED to 2.01c** — the 4+4+4 placeholder reviews/posts/topics remain so the site still renders content (12 `[PLACEHOLDER]` docs remain, down from 14). Shipped: surgical schema edits (review titles mk-optional, `coverImage` optional, `source` = firstPublished attribution; new `author.translations[]` + `education`) + typegen; an **idempotent** `scripts/import-content.mts` (+ `npm run import:content`, ran `created=0 updated=2`); a Style A **About "Translations" block** (8 entries, localized) + a quiet education line; the **live contact email** (`plecicdalibor@gmail.com`, mailto on Contact + footer) + **3 YouTube interview links**; finalized SEO `metadata` (dropped the unconfirmed "journalist") + JSON-LD (`jobTitle` minus Journalist; Person guards intact). ESLint now ignores the 2.04 `dist/` build artifact (was OOM-crashing lint). Zaporožac: 0 in data + built HTML. `typegen`/`lint`/`build` clean (58/58 static pages); axe 0 in page content on About/Book/review (the 10 footer flags are a verified axe bg-detection false positive — real cream-on-walnut ≈8:1). Code-review (2 subagents): no blocker/high. **Still pending:** 2.01c reviews/posts/topics import (needs the packet) + placeholder cleanup; the semantic backfill (post-Voyage-payment); the Formspree recipient switch; the portrait + book-cover images; the exact Литература.мк product URL; launch-QA copy sign-off. **No deploy / no Vercel env touched.** Report: `Part-2-Phase-01b-Completion.md`. — *(Prior: 2026-06-19)* **Phase 2.04 (Code) complete — Sanity Studio is DEPLOYED + LIVE at `https://daliborplecic.sanity.studio/`.** Dalibor now has a stable, permanent login that does **not** depend on the (not-yet-live) public site or its Vercel preview. Detection: the Studio is **embedded** (`src/app/studio/[[...tool]]`, second root layout) **and** the repo already has root `sanity.config.ts` + `sanity.cli.ts` — so `npx sanity deploy` deploys a **hosted standalone** Studio directly (no extra/minimal config scaffolded). **No `basePath` change was needed** (verified in the installed CLI source, not assumed): the hosted build's basePath = `determineBasePath()` → reads only `SANITY_STUDIO_BASEPATH` / `sanity.cli.ts` `project.basePath` (both unset) → `/`, and the generated entry passes `renderStudio(..., {basePath:"/"})` as an explicit override, so the embedded `/studio` basePath does **not** leak into the hosted build. **Confirmed targets:** `sanity schema list` → workspace `default` / dataset **`production`** / project **`ndqmaath`** (1/1 schemas deployed); CLI account `dinovlazar2011@gmail.com`. **CORS:** `https://daliborplecic.sanity.studio` added **with credentials** (localhost:3000 + :3333 already present). **Verified live:** root → 302 → Sanity dashboard auth → 200 (appId `mesr4xa2evz7kwohj4488y94`, no basePath 404); a `production` data query proves all five types exist with per-language fields (`{author:1,book:1,post:4,review:4,topic:4}`; review title `{mk,en,sr}` all present). **`sanity.cli.ts`** now pins `deployment.appId` (non-interactive future deploys). **`docs/dalibor-publishing-guide.md`** written (non-technical: login → 3-language model w/ graceful fallback → create post/review → cover+alt+book metadata → draft vs publish + imported-drafts note → review-search auto-reindex one-liner; MK version placeholder at top, not machine-translated). **New-Mac env fix (security):** stray no-dot `env.local` (held REAL secrets — Voyage/Supabase/webhook — and was NOT git-ignored, since `.env*` doesn't match it) + duplicate `gitignore` were left by dot-stripping on transfer, while the dotfile `.env.local` held the empty template; restored real values into `.env.local` (now ignored) and deleted both strays — no secret in any tracked file. **Human follow-ups (NOT code):** (1) invite Dalibor as an editor in the `ndqmaath` Members settings (permissions/dashboard action — Lazar/Cowork); (2) the final logged-in in-Studio visual click-through (schema + data already proven programmatically). Report: `Part-2-Phase-04-Completion.md`. — *(Prior: 2026-06-15)* **Phase 2.03 (Code) complete — Reviews semantic ("search by meaning") search is LIVE locally.** The Supabase vector store was created (migration `0001_review_embeddings.sql` applied in the **`dalibor-web`** project, ref **`wjqgkauzjrgnamacldgx`** — its ref matches `SUPABASE_URL`): `pgvector` + `review_embeddings` (`vector(1024)`) + HNSW cosine index + `match_reviews` RPC. **No Supabase MCP** was available and the service-role key can't run DDL via PostgREST, so the SQL was applied by the operator in the Supabase SQL editor; the table, RPC, and 1024-dim column were then verified from here via a service-role round-trip + the ranking test. **Model/dimension FINAL: `voyage-3.5` @ 1024** (confirmed live; `query` vs `document` `inputType`; `voyage-4` considered, 3.5 kept — proven + operator-set). **Backfill** (`npm run embed:reviews` → `scripts/embed-reviews.mts`): all **4** placeholder reviews embedded → 4 rows (`rows==reviews`); **idempotent** (re-run embedded=0/skipped=4 via a content hash) + orphan-prune (with an empty-dataset safety guard). **Semantic proven:** `semanticConfigured()` true; `POST /api/reviews/search` returns **`mode:"semantic"`** with the `ReviewSummary` shape intact; latency ~1.2s warm / ~2.2s cold (dev; the free-tier Voyage embed dominates). **Ranking proven content-independently** (`npm run test:semantic` → `scripts/test-semantic-ranking.mts`): 4/4 multilingual fixtures incl. a **cross-lingual** case (EN query → MK snippet, 0.572 vs ~0.30) and a **no-verbatim-keyword** case (0.577); fixtures live in a `zfixture-` namespace, torn down. **Keyword fallback proven intact both ways** — keys-unset → `keyword`, and keys-present-but-Voyage-invalid (a RUNTIME error, caught) → `keyword`; the search box never dies. **Re-index hook LIVE** (`/api/reviews/reindex`): missing/wrong `x-webhook-secret` → 401, correct → 200 + the row's `updated_at` refreshed (proven with a simulated webhook POST). A shared `src/lib/search/review-embedding-text.ts` builder makes the reindex route + the backfill embed **identically**; `embeddings.ts` gained `embedQueries`; `tsx` added (devDep) to run the `.mts` scripts. `lint` + `build` (`--webpack`) clean; code-review subagent pass done (no blocker/high; the one MEDIUM — an empty-dataset prune guard — was fixed). **Key finding (Cowork/launch):** the Voyage account has **no payment method → reduced limits 3 RPM / 10K TPM** (the 200M free `voyage-3.5` tokens still apply) — this will block the real ~78-review 2.01 backfill in one pass and can intermittently 429 live search (→ graceful keyword fallback). **Cowork hand-off:** (1) add a **Voyage payment method** (stays free under 200M tokens) before the 2.01 backfill + launch; (2) set the four search vars (`VOYAGE_API_KEY`/`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`SANITY_WEBHOOK_SECRET`) on the **Vercel** project at/after deploy — until then production runs the keyword fallback **by design, not a bug**; (3) register the **Sanity webhook** → `POST https://<prod-domain>/api/reviews/reindex`, header `x-webhook-secret: <SANITY_WEBHOOK_SECRET>`, on review create/update/publish; (4) run `npm run embed:reviews` **once** after the 2.01 import. The four keys live only in gitignored `.env.local` (no secret in any tracked file). Report: `Part-2-Phase-03-Completion.md`. — *(Prior: 2026-06-13)* **Phase 2.05 (Code, preview pass) complete — Vercel deploy validated on real infra; env-gated `noindex` added; 3 env gaps surfaced + handed to Cowork.** The repo **is** imported into Vercel (project `dalibor-web`, team `dinovlazars-projects`, Node 24.x) and the **build SUCCEEDS** — 4 deployments READY. Validation URL: **`https://dalibor-web.vercel.app`** (a *noindexed validation deploy, NOT production*). **Key finding:** every push to `main` deploys to Vercel's **production target on real domains** (`dalibor-web.vercel.app` + custom `daddybor.optimind000.com`, `daliborac.vertexconsulting.mk`) — i.e. it's not an isolated preview — **and three optional env vars were never set on the Vercel project**, surfacing three real issues: (1) **not noindexed** (no robots tag/meta → an indexable placeholder site); (2) **`NEXT_PUBLIC_SITE_URL` unset** → canonical/hreflang/OG/sitemap/robots/JSON-LD all emit `http://localhost:3000`; (3) **`NEXT_PUBLIC_FORMSPREE_ENDPOINT` unset** → the Contact form renders in **preview/no-send** mode (no `action`). (Sanity vars **are** set — the build wouldn't succeed otherwise, and content renders.) **Fix committed (code):** a server-side **`PREVIEW_NOINDEX`** flag honored by the root metadata (site-wide `robots: noindex,nofollow`, inherited by every page) + `robots.txt` (`Disallow: /`); locally verified ON (noindex meta on `/mk` + deep `/en/about`; `Disallow: /`) and OFF (no regression). The other two issues need **no code** — the vars just must be set on Vercel. **On-infra Lighthouse (`lighthouse@12`, isolated):** **desktop 100/100/100/92**, **mobile 79/100/100/92** — both SEO 92 fail on the single `canonical` audit (the localhost canonical → returns to ~100 once `SITE_URL` is set). **The 1.12 deferred mobile-Performance unknown is RESOLVED: 79 on real infra** — and the 1.12 hypothesis (CDN/HTTP-2 would lift it) is **disproved**: Lighthouse mobile *simulates* Slow-4G/4×CPU on top of the real network, so a fast CDN barely moves the mobile score (desktop's lighter throttle = 100). The gap is one content-independent lab artifact — **LCP render-delay 3.3s on the Playfair header wordmark** (webfont swap); render-blocking none, CLS 0, TBT 260ms, A11y/BP 100. Design-preserving font levers were already exhausted in 1.12. **Decision (evidence-backed): keep the locked Style A typography; do not chase the lab number; re-validate via real content (2.01/2.03) + field data (CrUX) at launch (2.06).** **Rich Results:** JSON-LD present + valid + correct `@type`s on live HTML (`Person` Home/About, `Book`, `Article`+`BreadcrumbList` single review); `url`/`@id` are localhost until `SITE_URL` is set — Google's Rich Results Test to be run on the live URL by the operator after that. **Remaining for launch — handed to Cowork/Lazar (I cannot set Vercel env or redeploy here):** set on the Vercel project then redeploy → `PREVIEW_NOINDEX=true`, `NEXT_PUBLIC_SITE_URL=https://dalibor-web.vercel.app`, `NEXT_PUBLIC_FORMSPREE_ENDPOINT=https://formspree.io/f/xqeogowo`; then re-confirm noindex + non-localhost SEO + real-send Contact. **Production promote + real domain → 2.06; content → 2.01/2.03.** Report: `Part-2-Phase-05-Completion.md`. — *(Prior: 2026-06-13)* **Phase 2.02 (Code) complete — Contact form live-wired to Formspree.** The real endpoint (`NEXT_PUBLIC_FORMSPREE_ENDPOINT=https://formspree.io/f/xqeogowo`) is set in `.env.local` (gitignored) and documented as a placeholder in `.env.example`; the env-gate now resolves to the **real-send** path (the rendered `<form action>` is the live endpoint, **no preview notice**). The AJAX payload now also carries the **active locale** (mk/en/sr) and a **reply-to** via the conventional `email` field; the `_subject` line was sharpened to "New message from your website — {name}" (all 3 languages). **Verified:** a scripted test submission returned **HTTP 200 `{ok:true}`**; a honeypot-filled submission was **accepted-and-silently-dropped** (Formspree fakes a 200 to fool bots) — **inbox receipt is the human-confirmed step** (interim inbox `dinovlazar2011@gmail.com`); `lint` + `build` clean (58/58 static pages, type-check passed); **axe on Contact = 0 violations** (28 passes; the 1 color-contrast "incomplete" is the known benign aria-hidden-glyph + textarea-overlap noise); focus-first-error, success-panel focus, and the error `aria-live` all re-verified in a real browser (success/error paths exercised against a mocked fetch to avoid extra deliveries). **Deferred to go-live (2.05):** switch the recipient to Dalibor's real email (must be added + verified as a Formspree Linked Email) and only then publish his email on the site — the Contact + footer email slots stay **inert** (no interim address shown); set the live-domain allow-list. The `_next` no-JS redirect was intentionally skipped (no thank-you page exists; Formspree's default thank-you page is a fine confirmation for the rare no-JS submitter, and the JS path is primary). Report: `Part-2-Phase-02-Completion.md`. — *(Prior: 2026-06-09)* **Phase 2.01 (Cowork) public-source pass complete.** Import-ready content packet assembled at `content-packet/`: a Reviews+Posts workbook (`Dalibor-Content-Packet-Reviews-and-Posts.xlsx`, 78 review rows / 2 post rows, full text in per-language slots), a Singletons+Copy doc (`Dalibor-Content-Packet-Singletons-and-Copy.docx` — Author, Book, 17-topic taxonomy, draft copy, parked facts, intake checklist), an `assets/` manifest (portrait/book-cover/reviewed-book covers as URLs + rights), and `notes/` (interviews, book details, links). Gathered: 79 review pieces (72 Booksa full-text + 3 The Literary Review + 2 Beton + 1 Arsenijević EN-excerpt), 3 blog pieces, final link set, book public details, bio-facts notes, 10/15 prioritized reviewed-book covers. **All intake-dependent items carried as PENDING; the two parked facts (book format/Zaporožac; birthplace/nationality/Croatia) left UNRESOLVED.** Flag for import: no Macedonian text exists yet → `requireMk` needs handling. Report: `Part-2-Phase-01-Completion.md`. Awaiting Lazar + Dalibor sign-off + intake merge. — *(Prior: 2026-06-08)* **Phase 1.12 complete → PART 1 COMPLETE.** Site-wide SEO/metadata pass: per-locale `<title>` + meta description (new `metadata` message namespace) via a reusable `buildPageMetadata` helper, self canonical + mk/en/sr + x-default hreflang, Open Graph + Twitter, a branded photography-free OG card (`next/og`), JSON-LD (`Person`/`Article`/`BlogPosting`/`Book`/`BreadcrumbList`, with the no-birth/nationality + no-genre/format guards), native `sitemap.xml` + `robots.txt`, a localized `noindex` 404, and a full WCAG 2.2 AA pass (Language-of-Parts `lang`, 2.4.11 scroll-margin, 2.5.8 target sizes, contrast + heading-order fixes; axe = 0 violations). All absolute URLs are env-driven (`NEXT_PUBLIC_SITE_URL`, localhost fallback). **Lighthouse:** desktop 99–100 on all four categories; mobile ≥95 on Accessibility/Best-Practices/SEO, with **mobile Performance ~79–85 on localhost** (webfont-LCP + simulated-Slow-4G FCP, no CDN/HTTP-2 locally) **to be re-measured on the deployed domain in 2.05** (operator-agreed). The locked design is unchanged.

**Project:** Dalibor Plečić personal website — a trilingual (Macedonian default / English / Serbian), literary "well-made hardcover book" site consolidating his book reviews, blog, his own book, and an About page.

---

## Phase status
- **1.01 — Deep research:** ✅ complete (dossier held locally by Lazar; gitignored).
- **1.02 — Project scaffolding:** ✅ complete.
- **1.03 — Design system & visual direction:** ✅ complete. Handover + four HTML mockups in `docs/design-handovers/`.
- **1.04 — Languages + Routing Foundation:** ✅ complete. next-intl v4 `[locale]` shell (mk/en/sr), root → `/mk`, switcher, Playfair+Lora, Style A tokens, trilingual UI strings.
- **1.05 — Sanity CMS + content models:** ✅ complete. Embedded Studio at `/studio`, five content types with field-level `{mk,en,sr}` localization, TypeGen, placeholder seed, connect-to-site proof. Report: `Part-1-Phase-05-Completion.md`.
- **1.06 — Core layout & shared components:** ✅ complete. shadcn/ui initialized + themed to Style A (Base UI base); real Style A header (sticky, wordmark, nav w/ active indicator, restyled MK·EN·SR switcher, accessible mobile menu), Style A footer (link groups from `site-links` + copyright + Privacy), shared primitives (Container/Section/PageHeader), skip-to-content link, trilingual chrome; `contact`/`privacy` stubs. Temp top bar removed. Report: `Part-1-Phase-06-Completion.md`.
- **1.07 — Home page:** ✅ complete. Real Style A Home at `[locale]/page.tsx` (hero / featured-book band / latest 3 reviews / latest 3 posts) in mk/en/sr, composed from `components/home/*`. `author` singleton extended with `tagline` (mk-required) + `heroIntro`; 4 new typed `HOME_*` queries; `next/image` wired for `cdn.sanity.io` (remotePatterns); mk→en→sr fallback + "available in" note on cards; page-load reveal (reduced-motion-gated). Report: `Part-1-Phase-07-Completion.md`.
- **1.08 — About + Book pages:** ✅ complete. Real Style A **About** (`[locale]/about`) and **Book** (`[locale]/book`) pages in mk/en/sr (stubs gone), plus a shared **Portable Text renderer** (`components/portable-text.tsx`, `@portabletext/react`) reused by both and by later content pages. About: name `<h1>` + roles/tagline, two-column 4:5 portrait | bio (stacks portrait-first), bio via Portable Text. Book: title `<h1>` + double rule, 2:3 cover + "by …" credit + publisher·year + "where to find it" buttons (external, `rel=noopener noreferrer`), description via Portable Text — **no genre/format rendered**. One schema edit (`purchaseLinks[].url` now required); existing fields reused (`photo` as portrait; flat publisher/year; `tagline` for Home vs `description` for Book — Home unaffected); seeded `genre` **emptied** (discrepancy guard). `ABOUT_QUERY` added, `BOOK_QUERY` repurposed, `AUTHOR_QUERY` removed. `@portabletext/react` now a direct dep. Report: `Part-1-Phase-08-Completion.md`.
- **1.09 — Reviews list + topic search + single-review pages:** ✅ complete. Real Style A `/[locale]/reviews` (stub gone): Archive header, SSR `?topic=` chip filter (JS-free), progressively-enhanced keyword search box → own API route, stack of shared `ReviewCard` rows, mk→en→sr fallback + "available in" note. Real `/[locale]/reviews/[slug]`: back link/breadcrumb, `<h1>` + double rule, meta, **shared Portable Text body with the §3.6 drop cap**, sticky reviewed-book aside, foot topic chips, `generateStaticParams` + `notFound`. **Search backend built two-tier (semantic→keyword fallback, honest `mode` flag), all server-side; semantic is DORMANT behind an env-presence gate (keyword works with zero keys).** Supabase migration written but **not run**. Report: `Part-1-Phase-09-Completion.md`.
- **1.10 — Blog (list + single-post pages):** ✅ complete. Real Style A `/[locale]/blog` (proof route gone): Archive header, **SSR `?topic=` chip filter (JS-free, `aria-current`, only topics used by posts — no dead chips)**, single-column stack of the new shared `PostCard`, mk→en→sr fallback + "available in" note, empty state. **No keyword search box** (blog has none). Real `/[locale]/blog/[slug]`: back link/breadcrumb, `<h1>` + §6.16 double rule, date + availability note, **shared Portable Text body with the §3.6 drop cap**, foot topic chips → `?topic=`, `generateStaticParams` + `notFound` + minimal `generateMetadata`, **no book aside**. The `topic-filter` was **generalized** to a shared `components/topic-filter.tsx` (`basePath` prop) consumed by both Reviews + Blog (Reviews `?topic=` regression-confirmed). Three new typed post queries; `POSTS_QUERY` superseded; seed gained a mk+en-only post. Report: `Part-1-Phase-10-Completion.md`.
- **1.11 — Contact + Privacy:** ✅ complete. Real Style A **Contact** (`/[locale]/contact`): intro + two-column (accessible form left, `ContactLinks` from `site-links.ts` right; stacks form-first on mobile), with the privacy-policy link in the form's note. The **custom form** (no `@formspree/react`) is fully WCAG-2.2-AA wired (labels, `aria-invalid`/`aria-describedby`, focus-first-error, polite `aria-live` status, focused success panel, honeypot `_gotcha`), progressively enhanced (`method=POST`+`action`), and **env-gated** (`NEXT_PUBLIC_FORMSPREE_ENDPOINT` empty in Part 1 → preview notice, nothing sent; real send in 2.02). Real Style A **Privacy** (`/[locale]/privacy`): `<h1>` + §6.16 double rule + lede + six `<h2>`/`<p>` sections in a `max-w-prose` column (semantic HTML, **not** Portable Text). New Style-A `ui/input` + `ui/label` + `ui/textarea` (hand-rolled — no new dep). 1.06 stubs + the now-unused `common.comingSoon` strings removed. Report: `Part-1-Phase-11-Completion.md`.
- **1.12 — SEO / schema / accessibility / Lighthouse pass:** ✅ complete. Reusable `buildPageMetadata` (per-locale title/description from the new `metadata` namespace + canonical + mk/en/sr/x-default hreflang + OG/Twitter) on every page; branded `next/og` OG + Twitter card (no photography); JSON-LD `Person`/`Article`/`BlogPosting`/`Book`/`BreadcrumbList` (confidence guards: no birth/nationality/location on Person, no genre/format on Book; `sameAs` from `site-links`); native `sitemap.xml` (45 URLs, hreflang alternates) + `robots.txt` (proxy-excluded); env-driven base URL (`NEXT_PUBLIC_SITE_URL`); localized `noindex` 404; WCAG 2.2 AA (Language-of-Parts `lang`, SC 2.4.11/2.5.8, contrast + heading-order fixes; **axe 0 violations**); mobile menu → Framer `LazyMotion`. Lighthouse desktop 99–100 ×4; mobile ≥95 on a11y/BP/SEO; **mobile Performance ~79–85 on localhost → re-measure on the deployed domain (2.05)**. Report: `Part-1-Phase-12-Completion.md`.
- **PART 1 COMPLETE.**
- **2.01 — Gather & prepare Dalibor's real content (Cowork):** 🟡 public-source pass reported complete, **but the assembled `content-packet/` was never committed to the repo** (correcting the stale claim here): only Dalibor's intake answers exist (now relocated into the repo at `content-packet/intake/`). The reviews/posts workbook + topics docx + assets manifest are **absent** — they must be dropped into `content-packet/` for 2.01c. Report: `Part-2-Phase-01-Completion.md`.
- **2.01b — Merge intake + import real content (Code):** 🟢 **PARTIAL pass complete.** Schema edits + typegen; idempotent `scripts/import-content.mts`; **real Author + Book singletons + 8 translations LIVE in `production`**; About "Translations" block + education; live email + 3 interview links; finalized SEO/JSON-LD. **Deferred to 2.01c (packet absent):** reviews/posts/topics/covers import + `[PLACEHOLDER]` cleanup (12 placeholder docs remain). `lint`/`build` clean (58/58); axe 0 in page content; code-review no blocker/high; no deploy / no Vercel env touched. Report: `Part-2-Phase-01b-Completion.md`.
- **2.01c — Reviews/posts/topics import (Code):** ✅ **complete.** The content arrived as **three JSON files** (`content-packet/{topics,reviews,posts}.json`), not the anticipated xlsx — read with `readFileSync`+`JSON.parse` (no parser/dep). **20 reviews + 1 post** imported (Croatian titles in the `sr` slot; `source` = "first published on Booksa"; **no body/excerpt**, copyright-safe). **Reconciled onto Dalibor's live `t-*` taxonomy** (the live dataset had drifted — his 14 topics, 0 reviews/posts, 0 placeholders): the importer **maps** packet slugs → his `t-*` ids and creates only `t-essay` + `t-society-politics` (operator-approved after a halt-and-ask; `women-and-gender`→`t-womens-writing`). `post.source` schema added via shared `sourceField()`; single-post attribution renders; Reviews list chips filter to used topics. Import verified + idempotent; `typegen`/`lint`/`build` clean (97 static pages); axe 0; code-review no blocker/high. **No embeddings backfill (Voyage payment pending); no deploy / no Vercel env.** Report: `Part-2-Phase-01c-Completion.md`.
- **2.01d — Gather Dalibor's photos into the repo (Cowork):** ✅ **complete.** Assembled `content-packet/assets/` (manifest `assets.json` + `README.md`): his **author portrait** (`author/portrait.jpg`, 720², identity confirmed across Versopolis + Booksa) `use:true`, a 512² **favicon source crop** (`author/avatar-square.jpg`), the **`Буники` cover HELD** (`use:"hold"` — no genuine flat cover exists online, only an event promo poster), and empty reviewed-book/blog/banner arrays (deferred). No login/stock/AI images. Not committed by Cowork → committed in 2.01e. Report: `Part-2-Phase-01d-Completion.md`.
- **2.01e — Upload Dalibor's photos to Sanity + set the site favicon (Code):** ✅ **complete.** Portrait uploaded → **`author.photo`** (alt mk/en/sr) — renders on About + Home; **book cover left as placeholder** (held). **Site favicon** set (`src/app/icon.png` 512² + `apple-icon.png` 180² from the avatar; default `favicon.ico` + 5 scaffold SVGs deleted). New idempotent **`scripts/import-assets.mts`** (`npm run import:assets` — patch not `createOrReplace`, preserve-if-set, `--dry-run`/`--force`, Zaporožac scrub, tally) + **`scripts/make-favicon.mts`** + **`sharp`** devDep. Run `uploaded=1 set=1 held=1`; 2nd run a no-op (idempotent). `typegen`(no diff)/`lint`/`build` clean (**98 static pages** — icon-swap +1, not a regression); axe About+Book 0 content violations; icons served + head-linked, `favicon.ico` 404. **No deploy / no Vercel env / no schema change.** Branch `phase/2.01e-photos` pushed; **merge pending Lazar.** Report: `Part-2-Phase-01e-Completion.md`.
- **2.01g — Reviewed-book covers: download, manifest, import, verify (Code):** 🟡 **covers ready; import DEFERRED.** Downloaded + validated **19/20** genuine flat portrait covers into `content-packet/assets/reviewed-books/`; extended `assets.json` `reviewed_books[]` to **20 entries (19 `use:true` + 1 held #11)** with mk/en/sr alt + provenance. 3 sub-350 phase URLs (#6/#8/#13) **upgraded** to ≥350 same-art VBZ copies (not held); **#19 accepted at 300×455**. **`Буники` not on literatura.mk today** → `book_cover` stays held, `purchaseLinks` untouched. Validated (20 entries, docIds 1:1, scrub 0); `typegen`(no diff)/`lint`/`build`(98 pages) clean. **`npm run import:assets` NOT RUN — no `SANITY_WRITE_TOKEN` here; operator chose commit-without-import.** Render/live/axe follow the import. Branch `phase/2.01g-reviewed-covers` pushed; **merge pending Lazar**. Report: `Part-2-Phase-01g-Completion.md`.
- **2.02 — Wire the contact form to the live Formspree endpoint (Code):** ✅ complete. Endpoint set (`.env.local`) + documented (`.env.example` placeholder); env-gate → **real-send** (no preview notice); payload now carries name · email (reply-to) · subject · message · **locale** · `_subject` · `_gotcha`. Scripted test → HTTP 200 `{ok:true}`; honeypot-filled → silently dropped; `lint` + `build` clean; **axe = 0** on Contact; success/error/focus states intact. **Go-live remainder (2.05):** real recipient (Dalibor's verified Formspree Linked Email) + publicly-shown email (slots stay inert) + live-domain allow-list. Report: `Part-2-Phase-02-Completion.md`.
- **2.05 — Vercel preview validation pass (Code):** 🟢 **preview/verify pass complete (repo side).** Build confirmed **succeeding** on Vercel (4 prod-target deploys READY, Node 24.x); validation URL `https://dalibor-web.vercel.app` (*noindexed validation deploy, not production*). Verified on real infra + fixed in repo: added env-gated **`PREVIEW_NOINDEX`** (site-wide `noindex` meta + `robots.txt Disallow: /`, locally verified); recorded **on-infra Lighthouse** (desktop **100**/100/100/92 · mobile **79**/100/100/92 — SEO 92 = localhost `canonical`); **mobile-Performance unknown RESOLVED = 79** (webfont-LCP lab artifact, CDN doesn't lift the *simulated* mobile score — keep Style A, re-validate via CrUX at launch); **Rich Results** JSON-LD valid + correct `@type`s on live HTML. **Blocked on Cowork/Lazar to finish the DoD (I can't set Vercel env / redeploy here):** set `PREVIEW_NOINDEX=true` + `NEXT_PUBLIC_SITE_URL=https://dalibor-web.vercel.app` + `NEXT_PUBLIC_FORMSPREE_ENDPOINT=https://formspree.io/f/xqeogowo` on the project, redeploy, then re-confirm noindex + non-localhost SEO + real-send Contact. Report: `Part-2-Phase-05-Completion.md`.
- **2.03 — Reviews semantic search (Code):** ✅ **complete locally.** Migration applied (Supabase `dalibor-web` / ref `wjqgkauzjrgnamacldgx`); 4 reviews backfilled (idempotent + orphan-prune guard); `/api/reviews/search` → `mode:"semantic"` (~1.2s warm); multilingual ranking test passes (cross-lingual + no-verbatim-keyword); keyword fallback intact (keys-off **and** runtime-error); reindex hook auth proven (401/200 + `updated_at` refresh). `voyage-3.5` @ 1024. **Dormant on production until the four keys are set on Vercel** (graceful keyword fallback meanwhile). Cowork: set the Vercel keys + register the Sanity webhook at the prod reindex URL + add a Voyage payment method + run the backfill once after the 2.01 import. Report: `Part-2-Phase-03-Completion.md`.
- **2.04 — Deploy Sanity Studio + publishing handover (Code):** ✅ **complete.** Studio **live at `https://daliborplecic.sanity.studio/`** (hosted standalone from the existing root config; embedded `/studio` basePath verified not to leak into the hosted build). Targets `production`/`ndqmaath` (schema 1/1); CORS origin added w/ credentials. `docs/dalibor-publishing-guide.md` written (EN; MK version placeholder). `sanity.cli.ts` pins `deployment.appId`. Fixed a new-Mac env-file dot-stripping mix-up that left real secrets in a non-ignored `env.local`. **Human follow-ups (not code):** invite Dalibor as editor (Members settings); final logged-in in-Studio visual check (schema + `production` data already verified programmatically). Report: `Part-2-Phase-04-Completion.md`.
- Next → **Part 2** (the **semantic embeddings backfill** (`npm run embed:reviews`) once a Voyage payment method is added — the 20 new reviews are unembedded, search runs the keyword fallback meanwhile · **2.06 production promote + real domain + final field/Lighthouse check**). Still pending from Dalibor: **the real `Буники` book cover** (2.01e placed an interim portrait but left the book cover as a placeholder — no genuine flat cover exists online), an **optional hi-res/studio portrait** to replace the interim 720² one, **reviewed-book covers** (optional, deferred), and **pasting the review/post prose** in the Studio. The 2.03 search keys + the Sanity webhook are set on Vercel at/after deploy; the **Formspree recipient switch** to `plecicdalibor@gmail.com` is a Cowork step. **Invite Dalibor as a Sanity editor** (Members settings) if not already done.

## Tech stack (current)
*Locked plan: Next.js (App Router) · TypeScript · Tailwind · shadcn/ui · Lucide · Framer Motion · next-intl · Sanity · Formspree · Vercel AI SDK + Voyage + Supabase (pgvector) · Vercel · Playfair Display + Lora.*

**Installed now:** Next.js 16.2.7 · React 19.2.4 · TypeScript 5.9.3 · Tailwind CSS 4.3.0 · ESLint 9 · next-intl 4.13.0 · Sanity 5.30.0 + toolchain · shadcn/ui (Base UI `@base-ui/react` 1.5.0) · lucide-react 1.17.0 · framer-motion 12.40.0 · class-variance-authority 0.7.1 · clsx 2.1.1 · tailwind-merge 3.6.0 · tw-animate-css 1.4.0 · @portabletext/react 6.2.0 · **(1.09) ai 6.0.197 · voyage-ai-provider 4.0.0 · @supabase/supabase-js 2.107.0 · server-only 0.0.1** (+ `shadcn` 4.10.0 in devDeps). Exact pins + the AI-search decisions in `00_stack-and-config.md`.

## What exists now
- **Trilingual App Router shell** under `src/app/[locale]/` (mk/en/sr). `[locale]/layout.tsx` is a root layout (fonts, tokens, `<html lang>`, provider) that now mounts the **real Style A chrome**: skip-to-content link → `<SiteHeader>` → `<main id="content" tabIndex=-1>` → `<SiteFooter>`. No `src/app/layout.tsx` (next-intl pattern).
- **Style A chrome (1.06):** sticky header (cream@85% + 8px blur + hairline; wordmark→home; desktop nav with caramel active underline + `aria-current`; restyled MK·EN·SR switcher; accessible Base UI Dialog mobile menu with focus trap/return + Escape + reduced-motion-gated Framer entrance); walnut footer (4 link groups from `site-links` + copyright + Privacy link). Shared primitives `Container`/`Section`/`PageHeader` + `SkipToContent`. shadcn primitives `Button`/`Separator`/`Sheet` restyled to Style A; `cn` in `src/lib/utils.ts`; nav source in `src/lib/nav.ts`; provisional links in `src/lib/site-links.ts`; brand icons in `src/components/brand-icons.tsx`.
- **Embedded Sanity Studio** at `/studio` — `src/app/studio/` is a **second root layout** sibling (its own `<html>`/`<body>`), so it is not localized. `[[...tool]]/page.tsx` (server: metadata/viewport/`force-static`) + `Studio.tsx` (`'use client'` → `<NextStudio config>`).
- **Sanity config** at repo root: `sanity.config.ts` (Studio: schema, structure w/ singletons, vision) + `sanity.cli.ts` (CLI: api + typegen).
- **Content model** in `src/sanity/schemaTypes/`: `post`, `review`, `book` (singleton), `author` (singleton — now incl. `tagline` mk-required + optional `heroIntro` for the Home hero), `topic`, shared `blockContent`, reusable localized `image`, and `localizedString/Text/BlockContent`. Field-level localization = plain `{mk,en,sr}` objects (NOT the i18n-array plugin — see report). Validation: mk-required titles/tagline, required slug, alt-required-when-image-set, **`book.purchaseLinks[].url` required (1.08)**. `book.genre` exists (editable) but is intentionally **unseeded / never rendered** — the novel-vs-collection ambiguity is resolved with Dalibor in 2.01.
- **Data layer** `src/sanity/lib/`: `client.ts` (public read — published perspective, `useCdn`, **no token**), `image.ts` (`@sanity/image-url`), `queries.ts` (**14** typed `defineQuery`: **(1.09)** `REVIEWS_LIST` / `REVIEWS_SEARCH` (+body) / `REVIEW_BY_SLUG` / `REVIEW_SLUGS` / `TOPICS` — superseded the thin `REVIEWS_QUERY`; **(1.10)** `POSTS_LIST` / `POST_BY_SLUG` (+body) / `POST_SLUGS` — superseded the thin `POSTS_QUERY`; + `ABOUT_QUERY` + `BOOK_QUERY` + 4 Home-scoped `HOME_*`), `localize.ts` (`localizedValue` mk→en→sr + `availableLanguages` + **(1.09)** `availableInLabel` + `resolveTopics`). `env.ts` validates the public env vars.
- **Reviews search layer (1.09)** `src/lib/search/`: `types.ts` (the `ReviewSummary`/request/response contract), `reviews-search.ts` (orchestrator — semantic→keyword fallback, honest `mode`, `semanticConfigured()`), `keyword.ts` (diacritic/case-insensitive matcher + Portable-Text flattener), `embeddings.ts` (Voyage wrapper — sole access point, server-only), `supabase.ts` (server-only service-role client). Routes: `src/app/api/reviews/search/route.ts` (live keyword fallback) + `src/app/api/reviews/reindex/route.ts` (**dormant** — env-gated + secret, wired in 2.03). Migration `supabase/migrations/0001_review_embeddings.sql` written but **not run**.
- **`next/image` wired for Sanity** — `next.config.ts` `images.remotePatterns` allows `cdn.sanity.io/images/ndqmaath/**`; covers render via the optimizer (`fill` + `sizes` + 2:3 wrapper), with the graceful §6.11 placeholder when no image is set. Proof routes still use plain `<img>` (styled in their own phases).
- **TypeGen**: `schema.json` + `src/sanity/sanity.types.ts` (generated), `npm run typegen`; `client.fetch(QUERY)` is typed.
- **Seed** in `sanity/seed/`: `build-seed.mjs` (generator) → `seed.ndjson` (13 `[PLACEHOLDER]` docs) + `placeholder-cover.png`. Imported into the `production` dataset. **(1.08)** `author.bio` + `book.description` expanded to 3 `[PLACEHOLDER]` paragraphs each (mk/en/sr); `book.genre` **removed from the seed** (discrepancy guard).
- **Proxy** `src/proxy.ts` excludes `/studio` **and (1.12) `/sitemap.xml` + `/robots.txt`** from locale routing.
- **SEO / metadata / a11y layer (1.12):** `src/lib/seo/metadata.ts` (`buildPageMetadata`), `src/lib/seo/jsonld.ts` (+`src/components/seo/json-ld.tsx`), `src/lib/seo/og-fonts/*.ttf` (5 bundled static OFL TTFs for the OG card), `src/app/sitemap.ts`, `src/app/robots.ts`, `src/app/[locale]/opengraph-image.tsx` + `twitter-image.tsx`, `src/app/[locale]/not-found.tsx`. `src/sanity/env.ts` exports `siteUrl` (`NEXT_PUBLIC_SITE_URL`, optional, localhost fallback). `localize.ts` gained `resolvedLanguage`/`contentLang`/`contentLangFromList` (SC 3.1.2). `globals.css` gained `scroll-margin-top` (SC 2.4.11). The two single-content queries select `_updatedAt` (JSON-LD `dateModified`).
- i18n layer, fonts, tokens, switcher, UI strings — unchanged from 1.04.

## Pages built
- **Real Style A Home** per locale (`/mk`,`/en`,`/sr`) — 1.07. Hero (typographic-only launch variant + dormant photo variant), featured-book parchment band, latest 3 reviews (horizontal cards), from-the-blog (3 posts, 2-up grid). Pulls live from Sanity; localized with mk→en→sr fallback; cards link to canonical `/[locale]/reviews/[slug]` + `/[locale]/blog/[slug]` (404 until 1.09/1.10).
- **Real Style A About + Book** per locale — 1.08. **About** (`/[locale]/about`): name `<h1>` + roles eyebrow + tagline, two-column 4:5 portrait (**Dalibor's real interim portrait since 2.01e** — the graceful placeholder remains the fallback if `author.photo` is ever cleared) | bio via Portable Text (stacks portrait-first on mobile), quiet Contact link. **Book** (`/[locale]/book`): title `<h1>` + §6.16 double rule, 2:3 `Cover` + "by …" credit + `publisher · year` + "where to find it" external buttons (`rel=noopener noreferrer`), description via Portable Text — **renders no genre/format** (discrepancy guard). Both localized mk→en→sr; page-load reveal (reduced-motion-gated).
- **Real Style A Reviews list + single review** per locale — 1.09. **List** (`/[locale]/reviews`, stub gone): Archive eyebrow + `<h1>` + lede, SSR topic-chip filter (`?topic=<slug>`, JS-free, `aria-current`), progressively-enhanced keyword search box (POSTs to `/api/reviews/search`, swaps the list for client results with header/Clear/empty-state, no layout shift), single-column stack of shared `ReviewCard` "library rows"; mk→en→sr fallback + "available in" note; empty state. **Single** (`/[locale]/reviews/[slug]`): back link + breadcrumb, `<h1>` + §6.16 double rule, date + availability note, full body via the shared Portable Text renderer **with the §3.6 drop cap** (verified rendering), sticky reviewed-book aside (cover, eyebrow, title + alternate-script subtitle, author·year, publisher, original `source` link), foot topic chips → `?topic=`; `generateStaticParams` (3 locales × 4 reviews), `notFound()` on unknown slug, minimal `generateMetadata`.
- **Real Style A Blog list + single post** per locale — 1.10. **List** (`/[locale]/blog`, proof route gone): Archive eyebrow + `<h1>` + lede, SSR topic-chip filter (`?topic=<slug>`, JS-free, `aria-current`, **only topics used by posts**), single-column stack of shared `PostCard` rows; mk→en→sr fallback + "available in" note; empty state. **No keyword search box.** **Single** (`/[locale]/blog/[slug]`): back link + breadcrumb (Home → Blog → title), `<h1>` + §6.16 double rule, date + availability note, full body via the shared Portable Text renderer **with the §3.6 drop cap**, foot topic chips → `?topic=`; `generateStaticParams` (3 locales × 4 posts), `notFound()` on unknown slug, minimal `generateMetadata`. **No book aside** (single `max-w-prose` reading column). (The `reviews` + `blog` proof routes are both replaced now; `contact`/`privacy` stubs remain until 1.11.)
- **Real Style A Contact + Privacy** per locale — 1.11 (stubs gone). **Contact** (`/[locale]/contact`): `PageHeader` (title + intro), then two columns ≥ md — the accessible `ContactForm` left, `ContactLinks` (from `site-links.ts`) right; stacks form-first on mobile. The form (Name*/Email*/Subject/Message* + hidden honeypot) validates client-side, marks errors with `aria-invalid`/`aria-describedby` + moves focus to the first invalid field, announces submitting/error/preview through a polite `aria-live` region and focuses the success panel; **env-gated** submit (preview notice when `NEXT_PUBLIC_FORMSPREE_ENDPOINT` empty; AJAX to Formspree when set), progressively enhanced. **Privacy** (`/[locale]/privacy`): `<h1>` + §6.16 double rule + lede + six `<h2>`/`<p>` sections in a `max-w-prose` reading column (semantic HTML, not Portable Text). Both have minimal per-locale `generateMetadata`; the switcher moves between `/mk|/en|/sr` equivalents.
- **`/studio`** — embedded Sanity Studio (its own root layout; **no site chrome** — confirmed).

## Components built
- **`language-switcher.tsx`** — 1.04 logic, restyled to §6.4 in 1.06 (+ `className`/`onNavigate`).
- **Layout (1.06):** `layout/site-header.tsx`, `layout/primary-nav.tsx` (client), `layout/mobile-menu.tsx` (client), `layout/site-footer.tsx`, `layout/container.tsx`, `layout/section.tsx`, `layout/page-header.tsx`, `layout/skip-to-content.tsx`.
- **Home (1.07):** `components/home/` — `hero.tsx`, `featured-book.tsx`, `latest-reviews.tsx`, `review-card.tsx`, `from-the-blog.tsx`, `blog-card.tsx`, `section-heading.tsx`. All server components, self-localizing via `getTranslations`. Helpers: `lib/datetime.ts` (sr→sr-Latn dates), `lib/strings.ts` (`monogramOf`).
- **Shared `Cover` (promoted 1.09):** `components/cover.tsx` (was `home/cover.tsx`) — book-cover (§6.11): `next/image` (`fill`+`sizes`, 2:3) or the graceful parchment placeholder. Free of server-only deps, so it renders in client components too (the Reviews search results reuse it). Used by Home / Book / Reviews.
- **Reviews (1.09):** `components/reviews/` — `review-card.tsx` (shared **sync** library-row card, server+client safe; both the SSR list and the client search results render it), `reviews-list.tsx` (SSR list), `review-search.tsx` (`'use client'` search box wrapping the SSR list), `review-results.tsx` (`'use client'` results + §6.9 empty state), `review-book-aside.tsx` (the §7.4 reviewed-book card). *(The 1.09 `reviews/topic-filter.tsx` was generalized to the shared `components/topic-filter.tsx` in 1.10.)*
- **Shared topic filter (generalized 1.10):** `components/topic-filter.tsx` — SSR `?topic=<slug>` chip filter (§6.8) with a `basePath` prop (`/reviews` or `/blog`); "All" + per-topic links, active chip `aria-current`. Consumed by **both** Reviews and Blog.
- **Blog (1.10):** `components/blog/post-card.tsx` — async server `PostCard` (§6.7/§7.5): whole-card link, title/date/2-line excerpt, optional non-interactive topic chips (+N) + optional 72×72 square thumbnail (when a post has a cover), mk→en→sr fallback + "available in" note. (Home's `blog-card.tsx`/`from-the-blog.tsx` left untouched.)
- **Content (1.08, +1.09):** `components/portable-text.tsx` — shared Style A Portable Text renderer (`@portabletext/react`), **locale-agnostic** server component. Styles every `blockContent` block/list/mark (Playfair h2–h4, Lora paragraphs, §6.10 blockquote, §3.5 lists, §2.2 caramel links with focus ring + auto external-link new-tab/`rel`), applies the `max-w-prose` measure, returns `null` when empty. **(1.09) gained an opt-in `dropCap` prop** (adds `.article-body`, triggering the §3.6 drop cap CSS) — used by single reviews; About + Book pass no `dropCap` and are unchanged. Reused by About + Book + single review (and single blog/book later).
- **shadcn primitives (1.06, Style A):** `ui/button.tsx` (exports `buttonVariants`, reused on Home CTAs), `ui/separator.tsx`, `ui/sheet.tsx`. Plus `brand-icons.tsx`. **(1.11)** `ui/input.tsx`, `ui/label.tsx`, `ui/textarea.tsx` — **hand-rolled** Style-A form primitives (native elements; no new dep — see report), focus/error via colour + soft ring (no layout shift), error driven by `aria-invalid`.
- **Contact (1.11):** `components/contact/contact-form.tsx` (`'use client'` — the accessible, env-gated form + idle/submitting/success/error/preview states), `components/contact/contact-links.tsx` (server — Dalibor's links from `site-links.ts` with localized descriptions; email slot inert until 2.02).

## Integrations wired
- **next-intl** (routing/proxy/request/navigation) — 1.04.
- **Sanity** — embedded Studio, typed read client, TypeGen, seed. (Formspree → later.)
- **Reviews topic search (1.09 built → 2.03 LIVE locally)** — two-tier (semantic Voyage→Supabase pgvector, keyword fallback). **Semantic path is now ON locally** (keys in `.env.local`, migration applied, reviews embedded): `/api/reviews/search` returns `mode:"semantic"`, the **re-index route is live** (secret-checked), the Supabase migration is **applied**. Keyword path still runs with zero keys **and on any runtime error** (proven). `ai` + `voyage-ai-provider` + `@supabase/supabase-js` (+ dev `tsx`) installed; `/api` excluded from locale routing. **On production it stays keyword-only until the four keys are set on Vercel** (Cowork hand-off).

## How to run it locally
```bash
npm install            # first time only
npm run dev            # http://localhost:3000  (→ /mk).  Studio at /studio
npm run typegen        # regenerate schema.json + sanity.types.ts after schema changes
```
Other scripts: `npm run build`, `npm run lint`, `npm start`.
- `.env.local` (gitignored) holds `NEXT_PUBLIC_SANITY_PROJECT_ID` (=`ndqmaath`), `NEXT_PUBLIC_SANITY_DATASET` (=`production`), `NEXT_PUBLIC_SANITY_API_VERSION` (=`2026-06-06`). The **1.09 search vars** (`VOYAGE_API_KEY`/`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`SANITY_WEBHOOK_SECRET`) are left **empty** for Part-1 — Reviews search runs the keyword fallback automatically; set them in 2.03. Template: `.env.example`.
- Sanity seed (re-import if needed): `npx sanity dataset import sanity/seed/seed.ndjson --dataset production --replace`.
- `dev`/`build` use `--webpack` (the SWC-native/Turbopack constraint from 1.02 is unchanged).

## Open carryover items
- **Studio content visual check** — (2.04) the hosted Studio `https://daliborplecic.sanity.studio/` is live + verified programmatically (schema deployed to `production`/`ndqmaath`; a `production` query proves all types + mk/en/sr fields). The only remaining bit is the **logged-in browser click-through** (all types render, singletons appear once, mk/en/sr inputs editable, review cover + book metadata) — done naturally on first editor login. **Also pending (human, not code): invite Dalibor as an editor** in the `ndqmaath` Members settings.
- **`review.coverImage` is now OPTIONAL (2.01b)** — the old "cover required" rule was removed (most reviews have no cover; graceful placeholder renders). Real covers land with the 2.01c review import. (`reviewTitle`/`bookTitle` are also no longer mk-required — reviews fall back mk→en→sr; slug stays required.)
- **19 moderate `npm audit`** findings (transitive — Sanity toolchain + the 1.09 ai/supabase chains) — `--force` would break (downgrades); revisit on upstream bumps.
- **`Load more` (§6.18) + reading-time deferred (Reviews + Blog)** — not in the 1.09/1.10 DoD; the seed archives are tiny (4 reviews, 4 posts). Add when they grow. The `PostCard` supports an optional 72×72 cover thumbnail, but no seed post has a `coverImage` yet (real covers in 2.01).
- **критика vs рецензија** terminology still open (UI strings provisional, from 1.04; Home strings use "критики" to match the nav).
- **Sanity images via `next/image`** — ✅ done (1.07): `remotePatterns` for `cdn.sanity.io` in `next.config.ts`; Home covers go through the optimizer. The remaining proof routes still use plain `<img>` until their own phases.
- **Content refresh** — the build-time Sanity fetch is cached (keeps Home static `●`); after a seed re-import an *incremental* rebuild can serve stale data until `.next` is cleared (clean CI builds are unaffected). ISR/revalidate/webhooks deferred to a later phase.
- Styled header/footer/nav + shadcn/ui → ✅ done (1.06). Real pages: Home/About/Book/Reviews/Blog ✅ (1.07–1.10); Contact/Privacy ✅ (1.11). **SEO/metadata/hreflang/sitemap/robots/JSON-LD/OG/404/a11y → ✅ done (1.12).** Dalibor's live login + Studio deploy → ✅ done (2.04: `https://daliborplecic.sanity.studio/`). AI search: keyword fallback ✅ live (1.09); **semantic embedding path + re-index webhook + Supabase migration → 2.03** (real keys + content).
- **(1.12) SEO carryovers for Part 2:** (a) ✅ **RESOLVED (2.05)** — **mobile Lighthouse Performance measured on real Vercel infra = 79** (desktop = **100**); the gap is a content-independent **webfont-LCP lab artifact** (Playfair wordmark, render-delay 3.3s) that a CDN can't move because Lighthouse mobile *simulates* Slow-4G/4×CPU; design-preserving levers exhausted in 1.12 → **keep Style A, re-validate via real content (2.01/2.03) + CrUX field data at launch (2.06)** (a11y/BP = 100 both, SEO 92 only from the localhost canonical); (b) the **`metadata` namespace strings** (titles/descriptions, all 3 langs) are working copy → finalize in 2.01; (c) **Person residence** + **Book language/genre/format** deliberately not asserted/limited → confirm with Dalibor in 2.01; (d) **`NEXT_PUBLIC_SITE_URL`** — confirmed UNSET on Vercel in 2.05 (live site emits `localhost` URLs, fails the `canonical` audit) → **set on the project (validation: `https://dalibor-web.vercel.app`; launch: real domain) + redeploy** — handed to Cowork; (e) **Google Rich Results Test** → JSON-LD verified valid + correct `@type`s on live HTML (2.05); run the Google tool on the live URL after `SITE_URL` is set (operator).
- **Contact form → live in 2.02:** the real Formspree endpoint (`NEXT_PUBLIC_FORMSPREE_ENDPOINT`), the real contact email (Contact + footer email slots inert now), an optional reCAPTCHA, and an optional native `_next` thank-you redirect all land in 2.02. The form is built + validated + AJAX-verified now; only the endpoint is missing.
- **Privacy copy is a plain-language placeholder** pending Dalibor's review at launch QA (he's a translator — the right approver); **sr is Latin script** (open script question — transliterates in one pass if sr later flips to Cyrillic). No real "effective date" is shown yet (launch QA).
- **`site-links.ts` finalized (2.01b)** — real **email** (`plecicdalibor@gmail.com`) shown publicly as a **mailto** on Contact + footer (no longer inert); single `interview`/`interviewVis` slots replaced by **`interviews[]`** (3 YouTube links, rendered as a list on Contact + footer); X/Twitter confirmed absent; Instagram @daliborac + Booksa/Versopolis/Partizanska/LinkedIn/Facebook kept.
- **shadcn on Base UI (`base-nova`), not Radix** (current CLI default) — see `00_stack-and-config.md` for the deviation + globals.css reconciliation. `tw-animate-css` imported but currently unused.

## Known issues
- None blocking. (Webpack/WASM fallback unchanged; after deleting route files, clear `.next` to drop stale generated types.)
